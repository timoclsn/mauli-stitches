import { styled } from '../stitches.config';

const Label = styled('div', {
    fontSize: '$sm',
    px: '$md',
    py: '$sm',
    color: '$white',
    backgroundColor: '$purple',
    borderRadius: '$round'
});

export default Label;
