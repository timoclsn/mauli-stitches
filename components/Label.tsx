import { styled } from '../stitches.config';

const Label = styled('div', {
    fontSize: '$sm',
    px: '$md',
    py: '$sm',
    color: '$indigo2',
    backgroundColor: '$indigo9',
    borderRadius: '$round'
});

export default Label;
